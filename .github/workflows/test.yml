name: Test Deploy

on:
  push:
    branches:
      - main

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Confirm deploy trigger
        run: echo "Push detected. Deployment should happen on cPanel."
